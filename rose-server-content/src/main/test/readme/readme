1、nacos 用作服务发现：
（1）当使用kill pid方式，将某个服务停止后，需要5秒左右的时间，nacos才会感知到，其他服务也才会感知到
     所以，需要优雅停机的话：
        a、需要自己写实现
        b、需要在nacos服务列表中，手动进行服务下线操作
（2）关于nacos领域模型：Namespace、Group、Service、Cluster、Instance
2、ribbon 路由规则：
（1）默认下是轮训
（2）可以配置成随机
（3）当配置成根据权重路由后，权重值越大，被选中的概率越大
     注意：权重被配置成0，将会永远不会被调用到




=========================================
nacos 的集群配置